# ===========================================
# Tecfag I.A - .gitignore
# MANTENDO APENAS node_modules IGNORADO
# ===========================================

# Logs (pode ignorar logs temporários)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependências - ÚNICO item importante ignorado
node_modules

# Build temporário (dist pode ser reconstruído)
dist
dist-ssr

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Arquivos locais de desenvolvimento
*.local

# ===========================================
# ATENÇÃO: Os seguintes itens NÃO estão mais
# sendo ignorados para manter tudo no repo:
# - .env (variáveis de ambiente)
# - server/.env
# - server/prisma/*.db (banco de dados)
# - server/uploads/ (documentos)
# - server/backups/
# ===========================================
